services:
  - type: web
    name: ADS
    env: java
    plan: free
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/ads-admin-0.0.1-SNAPSHOT.jar
    envVars:
      - key: PORT
        value: 8081

